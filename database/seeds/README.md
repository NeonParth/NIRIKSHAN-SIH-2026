# Demo seed

Synthetic records are created by `python -m app.seed` (idempotent on DEMO-001).

All rows use `data_source=SYNTHETIC`. Names are labelled SYNTHETIC / Demo Ward and are not real MPLADS entities.
